version: '3.8'

services:
  frontend:
    build: .
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=http://localhost:3000
    networks:
      - cineweb-network
    volumes:
      - ./src:/app/src

networks:
  cineweb-network:
    external: true