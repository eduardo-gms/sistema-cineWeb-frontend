version: '3.8'

services:
  frontend:
    build: .
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=https://${CODESPACE_NAME}-3000.app.github.dev
    networks:
      - cineweb-network
    volumes:
      - ./src:/app/src

networks:
  cineweb-network:
    external: true
